import { YoutubeCategoryPipe } from './youtube-category.pipe';

describe('YoutubeCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new YoutubeCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
